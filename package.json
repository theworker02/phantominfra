{
  "name": "phantominfra",
  "version": "1.4.2",
  "private": true,
  "description": "Speculative zero-latency edge engine — predict, pre-execute, commit or roll back.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @phantominfra/web",
    "dev:edge": "npm run dev -w @phantominfra/edge",
    "build": "npm run build -w @phantominfra/runtime && npm run build -w @phantominfra/sdk && npm run build -w @phantominfra/adapters && npm run build -w @phantominfra/web && npm run check -w @phantominfra/edge",
    "demo": "npm run demo -w @phantominfra/runtime",
    "bench": "npm run bench -w @phantominfra/runtime",
    "test": "npm run build -w @phantominfra/runtime && npm run test -w @phantominfra/runtime && npm run test -w @phantominfra/edge && npm run test -w @phantominfra/adapters",
    "deploy:edge": "npm run deploy -w @phantominfra/edge"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED"
}
